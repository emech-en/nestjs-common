import { RequestTransactionInterceptor } from './request-transaction.interceptor';

describe('TransactionalInterceptor', () => {
  it('should be defined', () => {
    // expect(new RequestTransactionInterceptor()).toBeDefined();
  });
});
